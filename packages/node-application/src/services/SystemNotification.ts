import { SystemNotificationService } from '@pomoguru/client'

export class NodeNotificationService implements SystemNotificationService {
  showNotification (title: string, options?: NotificationOptions) {
    // No Operation
  }
}
