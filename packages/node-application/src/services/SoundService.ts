import { SoundService } from '@pomoguru/client'

export class NodeSoundService implements SoundService {
  playWorkEndSound () {
    // No Operation
  }

  playBreakEndSound () {
    // No Operation
  }
}
